version=2
:if:<Battle.won>=true
	:if:<Pokemon.count>>0 <and> <Pokemon.needexp(0)>>0
		@Pokemon.gainexp(0,<Register.value(System.Encounter.Experience)>)
		@Text.show(<Pokemon.name(0)> [<Pokemon.nickname(0)>] have gained~Additional <Register.value(System.Encounter.Experience)> EXP from battle.)
	:endif
	:if:<Pokemon.count>>1 <and> <Pokemon.needexp(1)>>0
		@Pokemon.gainexp(1,<Register.value(System.Encounter.Experience)>)
		@Text.show(<Pokemon.name(1)> [<Pokemon.nickname(1)>] have gained~Additional <Register.value(System.Encounter.Experience)> EXP from battle.)
	:endif
	:if:<Pokemon.count>>2 <and> <Pokemon.needexp(2)>>0
		@Pokemon.gainexp(2,<Register.value(System.Encounter.Experience)>)
		@Text.show(<Pokemon.name(2)> [<Pokemon.nickname(2)>] have gained~Additional <Register.value(System.Encounter.Experience)> EXP from battle.)
	:endif
	:if:<Pokemon.count>>3 <and> <Pokemon.needexp(3)>>0
		@Pokemon.gainexp(3,<Register.value(System.Encounter.Experience)>)
		@Text.show(<Pokemon.name(3)> [<Pokemon.nickname(3)>] have gained~Additional <Register.value(System.Encounter.Experience)> EXP from battle.)
	:endif
	:if:<Pokemon.count>>4 <and> <Pokemon.needexp(4)>>0
		@Pokemon.gainexp(4,<Register.value(System.Encounter.Experience)>)
		@Text.show(<Pokemon.name(4)> [<Pokemon.nickname(4)>] have gained~Additional <Register.value(System.Encounter.Experience)> EXP from battle.)
	:endif
	:if:<Pokemon.count>>5 <and> <Pokemon.needexp(5)>>0
		@Pokemon.gainexp(5,<Register.value(System.Encounter.Experience)>)
		@Text.show(<Pokemon.name(5)> [<Pokemon.nickname(5)>] have gained~Additional <Register.value(System.Encounter.Experience)> EXP from battle.)
	:endif
:endif
:end