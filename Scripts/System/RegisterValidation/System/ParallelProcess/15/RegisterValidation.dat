version=2
:if:<Register.registered(15\tower)>=false
	:if:<Register.registered(System.ErrorLogInit)>=false
		@Register.change(System.ErrorLog.ErrorID2,2)
		@Script.start(System\GlobalErrorLog)
		@Register.register(System.ErrorLogInit)
	:endif
	@Text.log([Register Validation result: "15\tower" does not exist])
:else
	:if:<Register.value(15\tower)>=1 <or> <Register.value(15\tower)>=2
	:else
		:if:<Register.registered(System.ErrorLogInit)>=false
			@Register.change(System.ErrorLog.ErrorID2,2)
			@Script.start(System\GlobalErrorLog)
			@Register.register(System.ErrorLogInit)
		:endif
		@Text.log([Register Validation result: "15\tower" cannot be less than 1 and more than 2. Current value: <Register.value(15\tower)>])
	:endif
:endif
:end